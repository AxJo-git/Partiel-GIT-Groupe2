{
  "name": "task-manager-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Scripts centralisés pour lancer les tests du frontend et du backend ainsi que les métriques associées",
  "scripts": {
    "test": "npm run test --prefix ../backend && npm run test --prefix ../frontend",
    "test:coverage": "npm run test:coverage --prefix ../backend && npm run test:coverage --prefix ../frontend"
  }
}

